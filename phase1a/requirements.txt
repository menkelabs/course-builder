# Phase 1A - Interactive tracing & SVG export (SegFormer + SAM)
# Install: pip install -e .  or  pip install -e ".[sam]"  or  pip install -e ".[all]"
# Aligned with phase1a/pyproject.toml

# Core (required)
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0
shapely>=2.0.0
geopandas>=0.14.0
svgwrite>=1.4.3
cairosvg>=2.7.0
click>=8.1.0
rich>=13.0.0
pydantic>=2.0.0
pyyaml>=6.0.0

# Optional: SAM (pip install -e ".[sam]")
# torch>=2.0.0
# torchvision>=0.15.0
# segment-anything @ git+https://github.com/facebookresearch/segment-anything.git

# Optional: GUI (pip install -e ".[gui]")
# matplotlib>=3.7.0
# PyQt5>=5.15.0

# Optional: Dev (pip install -e ".[dev]")
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.0.0
