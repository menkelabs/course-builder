# Phase 1 - QGIS/GDAL Terrain Preparation
# Core dependencies (pinned from .venv at project root)
#
# Note: This project uses a shared .venv at the project root.
# Dependencies are pinned to match the installed versions in that environment.
# To update: cd to project root and run: .venv/bin/pip freeze > archive/phase1/requirements.txt

# CLI and utilities
click==8.3.1
rich==14.2.0
pydantic==2.12.5
pydantic-core==2.41.5
pyyaml==6.0.3
requests>=2.31.0

# GIS and raster processing
rasterio==1.4.4
geopandas==1.1.2
numpy==2.2.6
shapely==2.1.2
pyproj==3.7.1
pyogrio==0.12.1
fiona>=1.9.0  # Installed as dependency of geopandas

# Core dependencies (transitive)
affine==2.4.0
attrs==25.4.0
certifi==2026.1.4
cffi==2.0.0
click-plugins==1.1.1.2
cligj==0.7.2
packaging==25.0
pandas==2.3.3
python-dateutil==2.9.0.post0
pytz==2025.2
typing-extensions==4.15.0
tzdata==2025.3

# QGIS - NOT available via pip
# QGIS must be installed separately (Desktop or Server)
# PyQGIS bindings come with QGIS Desktop installation
# For automation, use qgis_process CLI tool (included with QGIS)
# See: qgis_setup/QGIS_SETUP.md for installation instructions
# Or run: python -m phase1.qgis_setup.setup_qgis

# GDAL - typically installed via system package manager or conda
# Python bindings (rasterio, geopandas) are installed above
# GDAL library itself must be installed separately
# See: https://gdal.org/download.html

# Optional: dev (matches pyproject [dev])
pytest==9.0.2
# pytest-cov>=4.0.0
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.0.0
